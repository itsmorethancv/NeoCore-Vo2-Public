"""
Root features package for NeoCore-o2.

Each subfolder contains both:
  - Python files (.py) used by the backend (python/main.py)
  - TypeScript/TSX files (.ts/.tsx) used by the Electron frontend

Usage in main.py (sys.path is set to include project root):
  from features.timer.timer import TimerManager, parse_duration_from_text
  etc.
"""
